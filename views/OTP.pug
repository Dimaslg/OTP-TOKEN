extends layout

block content
    
     a.btn.btn-outline-primary.btn-block.text-center(href="/home" role="button") Volver al inicio
    h1 TOKEN 
    label #{otp.secret}

    h1 QR
    img(src=otp.qr)

    